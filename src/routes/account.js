const express = require('express');

const router = express.Router();
const upload = require('../config/multer');
const eventsController = require('../app/controllers/AccountController');




module.exports = router;
